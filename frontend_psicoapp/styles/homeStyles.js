import { StyleSheet } from "react-native";

export const homeStyles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        padding: 20
    },
    welcome: {
        fontSize: 20,
        marginBottom: 20
    },
    link: {
        color: '#007BFF',
        marginTop: 10,
        textAlign: 'center'
    }
});
